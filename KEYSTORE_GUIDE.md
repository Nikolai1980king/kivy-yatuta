# 🔐 Руководство по созданию Keystore

## Что такое Keystore?

Keystore - это файл с цифровым сертификатом, который используется для подписи Android приложений. Он нужен для:
- Публикации в RuStore/Google Play
- Обновления приложения (без него нельзя выпустить обновление!)
- Подтверждения авторства приложения

**⚠️ ВАЖНО:** Сохраните пароль в безопасном месте! Без него вы не сможете обновлять приложение.

## Как заполнить форму создания keystore

Когда запустите `./create_keystore.sh`, вам будет предложено ввести:

### 1. Пароль для keystore (Enter keystore password)
```
Введите пароль (минимум 6 символов, рекомендуется 8+)
Например: MySecurePass123!
```
Затем подтвердите пароль (Re-enter new password)

### 2. Информация о сертификате

**What is your first and last name?**
```
Ятута
или
Ваше Имя
```

**What is the name of your organizational unit?**
```
Ятута
или
Development
или просто нажмите Enter
```

**What is the name of your organization?**
```
Ятута
или
ООО Ятута
или просто нажмите Enter
```

**What is the name of your City or Locality?**
```
Москва
или ваш город
```

**What is the name of your State or Province?**
```
Москва
или ваш регион
или просто нажмите Enter
```

**What is the two-letter country code for this unit?**
```
RU
(обязательно две буквы!)
```

### 3. Подтверждение
```
Is CN=Ятута, OU=Ятута, O=Ятута, L=Москва, ST=Москва, C=RU correct?
  [no]:  yes
```

### 4. Пароль для ключа (Enter key password for <yatuta>)
```
Можно использовать тот же пароль, что и для keystore
Или нажмите Enter, чтобы использовать тот же пароль
```

## Пример полного диалога:

```
Enter keystore password: MySecurePass123!
Re-enter new password: MySecurePass123!

What is your first and last name?
  [Unknown]: Ятута

What is the name of your organizational unit?
  [Unknown]: Ятута

What is the name of your organization?
  [Unknown]: Ятута

What is the name of your City or Locality?
  [Unknown]: Москва

What is the name of your State or Province?
  [Unknown]: Москва

What is the two-letter country code for this unit?
  [Unknown]: RU

Is CN=Ятута, OU=Ятута, O=Ятута, L=Москва, ST=Москва, C=RU correct?
  [no]: yes

Enter key password for <yatuta>
        (RETURN if same as keystore password): [Enter]
```

## После создания

1. Файл `yatuta.keystore` будет создан в текущей директории
2. Откройте `buildozer.spec` и раскомментируйте строки:
```ini
android.keystore = yatuta.keystore
android.keystore_password = MySecurePass123!
android.keystore_alias = yatuta
android.keystore_alias_password = MySecurePass123!
```

3. Замените `MySecurePass123!` на ваш реальный пароль

## ⚠️ Безопасность

- **НЕ коммитьте** `yatuta.keystore` в git (он уже в .gitignore)
- **НЕ коммитьте** пароли в `buildozer.spec` в публичный репозиторий
- **Сохраните** пароль в менеджере паролей (KeePass, 1Password и т.д.)
- **Сделайте резервную копию** файла `yatuta.keystore` в безопасном месте

## Если забыли пароль

К сожалению, восстановить пароль невозможно. Придется:
1. Создать новый keystore
2. Опубликовать приложение как новое (не обновление)
3. Пользователям нужно будет удалить старое и установить новое

Поэтому очень важно сохранить пароль!


