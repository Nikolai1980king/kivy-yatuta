# 🚀 Быстрый старт - Сборка APK для RuStore

## Что уже готово:
✅ Иконка приложения (`icon.png`)  
✅ Presplash экран (`presplash.png`)  
✅ Настроенный `buildozer.spec`  
✅ Код приложения (`main.py`)  

## Шаги для сборки:

### 1. Создайте keystore (один раз)
```bash
./create_keystore.sh
```

Затем откройте `buildozer.spec` и раскомментируйте строки с keystore (укажите ваш пароль):
```ini
android.keystore = yatuta.keystore
android.keystore_password = ваш_пароль
android.keystore_alias = yatuta
android.keystore_alias_password = ваш_пароль
```

### 2. Соберите release APK
```bash
buildozer android release
```

**Время первой сборки:** 30-60 минут (скачивание зависимостей)

### 3. Найдите готовый APK
```
bin/yatuta-1.0.0-arm64-v8a-release.apk
```

### 4. Протестируйте на устройстве
```bash
adb install bin/yatuta-1.0.0-arm64-v8a-release.apk
```

### 5. Загрузите в RuStore
Следуйте инструкциям в `BUILD_RUSTORE.md`

## ⚙️ Настройка URL сайта

По умолчанию используется `https://ятута.рф`. Чтобы изменить:

**Вариант 1:** Измените в `main.py`:
```python
WEBSITE_URL = os.getenv('WEBSITE_URL', 'https://ваш-сайт.ru')
```

**Вариант 2:** Установите переменную окружения:
```bash
export WEBSITE_URL=https://ваш-сайт.ru
buildozer android release
```

## 📋 Требования для RuStore

- ✅ Подписанный APK (готов после шага 2)
- ✅ Иконка 512x512 (уже создана)
- ⚠️ Скриншоты (минимум 2, сделайте сами)
- ⚠️ Описание приложения (напишите сами)
- ⚠️ Политика конфиденциальности (URL)

Подробности в `BUILD_RUSTORE.md`


