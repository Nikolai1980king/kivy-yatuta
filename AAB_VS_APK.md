# 📦 AAB vs APK - Что делать с файлом

## Что у вас сейчас

У вас есть файл: `yatuta-1.0.0-arm64-v8a_armeabi-v7a-release.aab`

## Разница между AAB и APK

### AAB (Android App Bundle)
- Формат для **Google Play Store**
- Google автоматически создает APK для каждого устройства
- Меньший размер файла
- **Нельзя установить напрямую** на устройство

### APK (Android Package)
- Универсальный формат
- Можно установить напрямую на устройство
- Нужен для **RuStore** и других магазинов
- Больший размер файла

## Что делать с AAB файлом

### Вариант 1: Собрать APK (для RuStore)

Я уже добавил настройку `android.aab = False` в buildozer.spec.

Теперь соберите APK:

```bash
buildozer android release
```

Будет создан файл: `bin/yatuta-1.0.0-arm64-v8a_armeabi-v7a-release.apk`

### Вариант 2: Конвертировать AAB в APK (сложнее)

Можно использовать `bundletool` от Google:

```bash
# Скачать bundletool
wget https://github.com/google/bundletool/releases/download/1.15.6/bundletool-all-1.15.6.jar

# Конвертировать AAB в APK
java -jar bundletool-all-1.15.6.jar build-apks \
  --bundle=bin/yatuta-1.0.0-arm64-v8a_armeabi-v7a-release.aab \
  --output=bin/yatuta.apks \
  --ks=yatuta.keystore \
  --ks-pass=pass:Yatuta2024! \
  --ks-key-alias=yatuta \
  --key-pass=pass:Yatuta2024!

# Распаковать APK из архива
unzip bin/yatuta.apks -d bin/
```

Но проще просто пересобрать APK.

## Для RuStore

**Нужен APK файл!**

После сборки APK:
1. Файл будет в `bin/yatuta-1.0.0-arm64-v8a_armeabi-v7a-release.apk`
2. Загрузите его в RuStore
3. Размер будет около 30-40 МБ

## Для Google Play

**Нужен AAB файл!**

Если планируете публиковать в Google Play:
1. В buildozer.spec установите `android.aab = True`
2. Соберите AAB: `buildozer android release`
3. Загрузите AAB в Google Play Console

## Рекомендация

Для RuStore просто пересоберите APK:

```bash
buildozer android release
```

Это займет меньше времени, так как зависимости уже скачаны.


