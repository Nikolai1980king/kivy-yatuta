# 📱 Как посмотреть логи приложения

## Установка ADB

```bash
sudo apt install adb
```

## Подключение устройства

1. Включите "Отладка по USB" в настройках разработчика на телефоне
2. Подключите телефон к компьютеру
3. Проверьте подключение:
   ```bash
   adb devices
   ```

## Просмотр логов

### Все логи приложения:
```bash
adb logcat | grep -i "yatuta"
```

### Только ошибки:
```bash
adb logcat *:E | grep YatutaApp
```

### Сохранить логи в файл:
```bash
adb logcat > app_logs.txt
```

### Очистить логи и начать заново:
```bash
adb logcat -c
adb logcat | grep -i "yatuta\|error\|exception"
```

## Что искать в логах

- `YatutaApp` - сообщения от нашего приложения
- `ERROR` - ошибки
- `Exception` - исключения
- `FATAL` - критические ошибки

## Альтернативный способ (без ADB)

Установите на телефон приложение:
- **Log Viewer** (Google Play)
- **aLogcat** (Google Play)

Затем откройте приложение и найдите логи с тегом "YatutaApp".


