# 🐛 Отладка приложения

## Проблема: Приложение закрывается сразу после запуска

### Что было исправлено:

1. **Добавлена обработка ошибок** - теперь ошибки логируются и показываются пользователю
2. **Отложенная инициализация WebView** - WebView создается после полной готовности активности
3. **Fallback UI** - при ошибке показывается сообщение вместо закрытия приложения
4. **Логирование** - все ошибки записываются в Android Logcat

### Как посмотреть логи:

#### Вариант 1: Через ADB (если установлен)

```bash
# Установите ADB
sudo apt install adb

# Подключите устройство и включите отладку по USB

# Просмотр логов в реальном времени
adb logcat | grep -i "yatuta\|python\|kivy\|error"

# Или только ошибки
adb logcat *:E | grep YatutaApp
```

#### Вариант 2: Через приложение на телефоне

Установите приложение "Log Viewer" или "aLogcat" из Google Play для просмотра логов.

#### Вариант 3: Через Android Studio

1. Откройте Android Studio
2. Подключите устройство
3. Откройте вкладку "Logcat"
4. Фильтруйте по "YatutaApp"

### Пересборка APK с исправлениями:

```bash
# Пересоберите APK
buildozer android release

# Конвертируйте AAB в APK (если нужно)
./convert_aab_to_apk.sh
```

### Возможные причины ошибок:

1. **URL недоступен** - проверьте, что сайт `https://ятута.рф` доступен с устройства
2. **Проблемы с сертификатом SSL** - сайт должен иметь валидный SSL сертификат
3. **Проблемы с разрешениями** - проверьте, что разрешения INTERNET выданы
4. **Ошибка в коде WebView** - теперь будет показана в приложении

### Что делать дальше:

1. Пересоберите APK с исправленным кодом
2. Установите на устройство
3. Посмотрите логи через ADB или приложение
4. Если видите ошибку в логах - пришлите её для исправления

### Проверка URL:

Убедитесь, что URL правильный и доступен:

```bash
# Проверьте доступность сайта
curl -I https://ятута.рф

# Или в браузере на телефоне откройте этот URL
```

Если сайт недоступен, измените URL в `main.py`:

```python
WEBSITE_URL = os.getenv('WEBSITE_URL', 'https://ваш-рабочий-сайт.ru')
```


