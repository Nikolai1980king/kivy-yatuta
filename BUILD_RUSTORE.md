# 📱 Инструкция по сборке APK для RuStore

## Шаг 1: Подготовка keystore (только один раз)

Для публикации в RuStore нужен подписанный APK. Создайте keystore:

```bash
./create_keystore.sh
```

Или вручную:
```bash
keytool -genkey -v -keystore yatuta.keystore -alias yatuta -keyalg RSA -keysize 2048 -validity 10000
```

**⚠️ ВАЖНО:** Сохраните пароль от keystore! Без него вы не сможете обновлять приложение в RuStore.

## Шаг 2: Настройка buildozer.spec

Откройте `buildozer.spec` и раскомментируйте строки для keystore:

```ini
android.keystore = yatuta.keystore
android.keystore_password = ваш_пароль
android.keystore_alias = yatuta
android.keystore_alias_password = ваш_пароль
```

## Шаг 3: Сборка release APK

```bash
# Первая сборка (займет 30-60 минут)
buildozer android release

# Или если нужно пересобрать
buildozer android clean
buildozer android release
```

APK будет в папке `bin/`:
- `bin/yatuta-1.0.0-arm64-v8a-release.apk`

## Шаг 4: Требования для RuStore

Для публикации в RuStore вам понадобится:

### 1. Подписанный APK (release версия)
   - ✅ Уже готов после шага 3

### 2. Иконка приложения
   - Размер: 512x512 PNG
   - ✅ Уже создана: `icon.png`

### 3. Скриншоты приложения
   - Минимум: 2 штуки
   - Максимум: 8 штуков
   - Рекомендуемый размер: 1080x1920 (portrait) или 1920x1080 (landscape)
   - Формат: PNG или JPG

### 4. Описание приложения
   - Краткое описание (до 80 символов)
   - Полное описание (до 4000 символов)
   - Пример:
     ```
     Краткое: Мобильное приложение для доступа к сайту Ятута
     
     Полное: 
     Ятута - удобное мобильное приложение для доступа к вашему любимому сайту.
     Все функции сайта доступны прямо из приложения с удобным интерфейсом.
     ```

### 5. Политика конфиденциальности
   - URL на вашу политику конфиденциальности
   - Должна быть доступна по HTTPS

### 6. Категория приложения
   - Выберите подходящую категорию (например, "Интернет", "Социальные сети")

### 7. Возрастной рейтинг
   - Укажите возрастной рейтинг приложения

## Шаг 5: Публикация в RuStore

1. Зарегистрируйтесь как разработчик на [RuStore](https://www.rustore.ru/)
2. Создайте новое приложение
3. Загрузите APK файл
4. Заполните все необходимые поля:
   - Название приложения
   - Описание
   - Иконка (512x512)
   - Скриншоты (минимум 2)
   - Политика конфиденциальности
   - Категория
   - Возрастной рейтинг
5. Отправьте на модерацию

## 🔧 Полезные команды

```bash
# Проверить подпись APK
jarsigner -verify -verbose -certs bin/yatuta-1.0.0-arm64-v8a-release.apk

# Установить APK на устройство через ADB
adb install bin/yatuta-1.0.0-arm64-v8a-release.apk

# Очистить кеш сборки
buildozer android clean

# Посмотреть информацию о APK
aapt dump badging bin/yatuta-1.0.0-arm64-v8a-release.apk
```

## ⚠️ Важные замечания

1. **Keystore пароль** - сохраните в безопасном месте! Без него нельзя обновлять приложение
2. **Версия приложения** - при обновлении увеличьте версию в `buildozer.spec` (version = 1.0.1)
3. **URL сайта** - должен быть доступен по HTTPS и с мобильного интернета
4. **Тестирование** - обязательно протестируйте APK на реальном устройстве перед публикацией

## 🐛 Решение проблем

### Ошибка при сборке
```bash
buildozer android clean
buildozer android release
```

### APK не устанавливается
- Проверьте, что устройство поддерживает архитектуру APK (arm64-v8a)
- Убедитесь, что включена установка из неизвестных источников

### WebView не загружает сайт
- Проверьте URL в `main.py`
- Убедитесь, что сайт доступен по HTTPS
- Проверьте разрешения INTERNET в buildozer.spec


